import { GeorgianDatePipe } from './georgian-date.pipe';

describe('GeorgianDatePipe', () => {
  it('create an instance', () => {
    const pipe = new GeorgianDatePipe();
    expect(pipe).toBeTruthy();
  });
});
